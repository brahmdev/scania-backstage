apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: all
  description: Location of all git controlled resources
spec:
  targets:
    - ./../catalog-info.yml
    - ./all-apis.yml
    - ./all-groups.yml
